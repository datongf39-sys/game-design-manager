# 首次部署后使用说明

## 🎉 部署成功！

当你第一次访问部署到 Render 的应用时，可能会看到 404 错误或其他错误。这是正常的！

## ⚠️ 重要提示

### 这是一个纯前端应用

- **所有数据存储在浏览器的 LocalStorage 中**
- **不同设备/浏览器之间数据不共享**
- **首次访问时 LocalStorage 是空的**

### 为什么看到 404 错误？

如果你访问的 URL 包含项目 ID（如 `/project/proj_xxx`），但 LocalStorage 中没有这个项目，就会看到 404。

**解决方案**：

1. **访问根路径**：
   ```
   https://game-design-manager.onrender.com/
   ```

2. **清除浏览器缓存**（如果遇到问题）：
   - Windows: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`

3. **创建你的第一个项目**：
   - 在首页点击"新建项目"
   - 输入项目名称、选择颜色
   - 点击"创建"

## 📱 多设备使用

由于数据存储在 LocalStorage：

- ✅ 在**同一浏览器**中，数据会保持
- ❌ 在**不同浏览器**中，数据不共享
- ❌ 在**不同设备**上，数据不共享
- ❌ **清除缓存**后，数据会丢失

### 建议

1. **固定使用一个浏览器**访问应用
2. **不要清除浏览器缓存**
3. **定期导出数据**（未来功能）
4. **考虑使用浏览器的"固定标签页"功能**

## 🚀 开始使用

### 步骤 1：创建项目
1. 访问首页
2. 点击"新建项目"
3. 输入项目名称（如"我的游戏"）
4. 选择项目颜色
5. 点击"创建"

### 步骤 2：创建模块
1. 进入项目页面
2. 点击"添加模块"
3. 输入模块名称（如"物品"、"角色"）
4. 选择模块图标
5. 点击"创建"

### 步骤 3：配置字段
1. 点击模块的"字段配置"按钮
2. 添加需要的字段：
   - 单行文本
   - 多行文本
   - 富文本
   - 数字
   - 日期
   - 单选/多选
   - 关联（跨表引用）
3. 保存配置

### 步骤 4：添加记录
1. 点击"添加记录"
2. 填写字段内容
3. 点击单元格编辑
4. 失焦自动保存

### 步骤 5：使用高级功能
- **层级结构**：在详情面板设置父级记录
- **分组视图**：按字段分组查看
- **富文本编辑**：点击富文本字段打开编辑器
- **跨表引用**：创建 relation 字段关联其他模块

## 🔧 故障排查

### 问题 1：看到 404 错误
**原因**：URL 中有项目 ID，但 LocalStorage 中没有该项目

**解决**：
1. 访问根路径 `/`
2. 创建新项目

### 问题 2：页面空白
**原因**：可能是浏览器缓存问题

**解决**：
1. 硬刷新：`Ctrl + Shift + R`
2. 清除浏览器缓存
3. 检查浏览器控制台错误

### 问题 3：数据丢失
**原因**：清除了浏览器缓存

**解决**：
1. 数据无法恢复（纯前端应用的限制）
2. 考虑未来添加数据导入/导出功能
3. 定期截图或记录重要数据

### 问题 4：无法访问 Render
**原因**：Render 免费套餐有每月带宽限制

**解决**：
1. 等待下个月
2. 或升级到付费套餐

## 📊 数据迁移（未来功能）

计划添加：
- 导出数据到 JSON 文件
- 从 JSON 文件导入数据
- 备份到云存储

## 🆘 获取帮助

如果遇到问题：
1. 打开浏览器开发者工具（F12）
2. 查看控制台错误信息
3. 查看 Network 标签页的网络请求
4. 联系开发者

## 🎯 下一步

1. ✅ 创建你的第一个项目
2. ✅ 添加模块和字段
3. ✅ 开始记录游戏设计文档
4. ✅ 探索所有功能
5. ⏳ 等待数据导入/导出功能

祝你使用愉快！🎮
